<!--
Daily evening retro report — canonical schema.

Each `<project>/knowledge/reports/<YYYY-MM-DD>.md` file is generated by
`kb-orchestrator-cycle retro` (Phase 3) for the project's evening cycle.
Parents read children's reports and roll them up into their own report.

The frontmatter is REQUIRED — `kb-orchestrator-cycle` parses it for state
machine transitions (status enum) and roll-up aggregation. Don't edit the
status field by hand; let the orchestrator manage lifecycle.

Status enum (CR2-N3):
  pending    — terminal state written before cycle attempt; replaced by spawn
  done       — full retro completed, all sections populated
  partial    — agent finished but some sections missing or unclear
  timeout    — broker timeout exceeded SLA
  error      — broker returned non-zero exit, content uncertain
  skipped    — cycle_enabled=false on this project; no spawn attempted

`children_rolled_up` — list of child slugs whose reports parent read in.
Empty for leaves. Aggregation is bottom-up: leaves first (level 0), then
parents merge child Candidates/Escalations sections.
-->
---
report_id: <slug>-<YYYY-MM-DD>
slug: <slug>
date: YYYY-MM-DD
cycle: evening
parent: <parent-slug-or-null>
children_rolled_up: []
status: pending
run_id: <uuid>
---

## Что сделано сегодня

- <bullet, ref на backlog:lineno или log entry>

## Что было в плане

- <из daily-plan/<YYYY-MM-DD>.md или top-down плана от parent>

## Дельта (план − факт)

- closed: N
- dropped: <slug>:<title> — <причина>
- carried: <slug>:<title> — <когда>

## Candidates (что можно сделать завтра)

- [ ] <задача> — P0|P1|P2 — auto: true|false — <обоснование>

## Escalations (наверх)

- <блокер> — требует: <parent-slug|owner|external>
