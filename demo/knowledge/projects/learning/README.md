# learning — books, courses, ingestion

Long-form reading + occasional structured courses. Not "passive
consumption" — every book gets a `sources/<slug>.md` summary; insights
that apply across projects get lifted into hub-level `concepts/`.

Status: **live** (active ingestion stream — DDIA finished, three more in
flight, source summaries produced for each).
